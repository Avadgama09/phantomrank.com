---
// src/layouts/PillarLayout.astro — Hero image + two-column: prose content (left) + HubNav sidebar (right)
import Layout from "./Layout.astro";
import HubNav from "../components/content/HubNav.astro";

interface ClusterLink {
  slug: string;
  title: string;
  href: string;
}

interface Props {
  title: string;
  description?: string;
  breadcrumbs?: { name: string; href?: string }[];
  hubTitle: string;
  clusters: ClusterLink[];
  featuredImage?: { src: string; alt: string };
}

const { title, description, breadcrumbs, hubTitle, clusters, featuredImage } = Astro.props;
---

<Layout title={title} description={description} breadcrumbs={breadcrumbs}>
  <div class="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8">

    {featuredImage?.src && (
      <div class="mb-10 overflow-hidden rounded-2xl">
        <img
          src={featuredImage.src}
          alt={featuredImage.alt}
          class="h-auto w-full object-cover"
          loading="eager"
          decoding="async"
        />
      </div>
    )}

    <div class="lg:grid lg:grid-cols-[1fr_280px] lg:gap-12">
      <article
        class="prose prose-lg max-w-none
          prose-headings:font-semibold prose-headings:tracking-tight prose-headings:text-foreground
          prose-p:text-muted-foreground prose-p:leading-8
          prose-a:text-primary prose-a:no-underline hover:prose-a:underline
          prose-strong:text-foreground
          prose-li:text-muted-foreground
          prose-blockquote:border-primary/40 prose-blockquote:text-muted-foreground
          prose-code:rounded prose-code:bg-muted prose-code:px-1.5 prose-code:py-0.5 prose-code:text-primary
          prose-table:text-sm
          prose-th:font-semibold prose-th:text-foreground
          prose-td:text-muted-foreground"
      >
        <slot />
      </article>

      <aside class="hidden lg:block">
        <HubNav hubTitle={hubTitle} clusters={clusters} />
      </aside>
    </div>
  </div>
</Layout>
